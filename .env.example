# LLM Provider Keys
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# Redis Configuration (Optional for caching)
REDIS_HOST=localhost
REDIS_PORT=6379

# Logging
LOG_LEVEL=INFO

# Bunny Stream (course video embeds)
NEXT_PUBLIC_BUNNY_LIBRARY_ID=
NEXT_PUBLIC_BUNNY_PULL_ZONE_ID=
# Embed view token key (Stream → Library → Security → Embed view token authentication)
BUNNY_EMBED_TOKEN_KEY=
